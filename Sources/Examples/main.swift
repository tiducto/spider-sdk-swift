import Foundation
import SpiderSDK

// A trivial entry point so this target links as an executable. The example functions in the sibling files are
// documentation sources — they type-check against the real SDK but are intentionally never invoked here.
print("spider-sdk-examples: see the *.swift files in this target for region-tagged, documented SDK usage")
